import SignInButton from "./SignInButton";

const NavBar = () => {
  return (
    <div className="bg-slate-100">
      NAVIGATION BAR
      <SignInButton />
    </div>
  );
};

export default NavBar;
